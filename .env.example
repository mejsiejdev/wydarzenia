DATABASE_URL=postgresql://user:password@localhost:5432/database_name
JWT_SECRET_KEY=
# Optional: a SEPARATE, empty database used only by the test suite (pytest).
# The tests wipe their data via per-test rollback, but never point this at a
# database you care about.
TEST_DATABASE_URL=postgresql://user:password@localhost:5432/wydarzenia_test
